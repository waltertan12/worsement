// import { connect, onAccountUpdate, onOrderUpdate } from './service/brokerage';

/*
connect('trade', () => console.log('Connected'));
onOrderUpdate((event, order, timestamp) => {
    console.log(event);
    console.log(order);
    console.log(timestamp);
});
onAccountUpdate((event, account, timestamp) => {
    console.log(event);
    console.log(account);
    console.log(timestamp);
});
*/
